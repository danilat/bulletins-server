extends ./layout.jade

block title
  title= "Actualizar noticia"

block content
  h2 Actualizar noticia
  div(class="well bs-component")
    form(action="/bulletins/"+ bulletin.id, method="post", class="form-horizontal")
      fieldset
        div(class="form-group")
          label(class="col-lg-2 control-label") Título
          div(class="col-lg-10")
            input(class="form-control", type="text", name="name", value=bulletin.name)
        div(class="form-group")
          label(class="col-lg-2 control-label") Cuerpo
          div(class="col-lg-10")
            textarea(class="form-control", name="body", rows="15") #{bulletin.body}
        div(class="form-group")
          div(class="col-lg-10 col-lg-offset-2")
            input(type="submit", value="Actualizar", class="btn btn-primary")
            a(href="/", class="btn btn-default") Volver al listado